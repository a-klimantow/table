import {
  Search,
  Cancel,
  ViewColumn,
  FilterList,
  Visibility,
  VisibilityOff,
} from '@material-ui/icons'

export default {
  search: Search,
  search_clear: Cancel,
  col_menu: ViewColumn,
  filter_list: FilterList,
  eye_on: Visibility,
  eye_off: VisibilityOff,
}
